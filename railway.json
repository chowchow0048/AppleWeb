{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "startCommand": "cd appleWeb && python manage.py migrate --noinput && python manage.py collectstatic --noinput && gunicorn config.wsgi:application -c ../gunicorn.conf.py",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
